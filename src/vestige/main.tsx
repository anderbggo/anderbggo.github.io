import React from "react";
import ReactDOM from "react-dom/client";
import VestigeApp from "./VestigeApp";
import "./vestige.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <VestigeApp />
  </React.StrictMode>,
);